import "./Home.css";

import Card from "../UI/Card/Card";
/**
 *
 * @param {*} props
 * @returns
 */
const home = (props) => (
  <Card className="home">
    <h1>Welcome Back</h1>
  </Card>
);

export default home;
